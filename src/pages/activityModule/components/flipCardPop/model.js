import { saveHomeStyle, saveHomeAdStyle, backStageSActivityThree } from "@/services/activity";
const Model = {
  namespace: 'flipCardPop',
  state: {
  },
  effects: {
  },
  reducers: {
  },
};
export default Model;
